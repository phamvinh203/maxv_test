generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Registration {
  id        String   @id @default(cuid())
  name      String
  phone     String
  email     String
  package   String
  createdAt DateTime @default(now())

  @@map("registrations")
}
